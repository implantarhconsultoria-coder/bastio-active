{
  "name": "bastio-active",
  "version": "1.0.0",
  "private": true,
  "description": "Bastio Active static storefront.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check assets/store.js && node --check assets/app.js && node --check assets/admin.js"
  },
  "engines": {
    "node": ">=18"
  }
}
